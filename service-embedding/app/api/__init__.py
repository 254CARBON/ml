"""API subpackage for the embedding service.

Contains FastAPI routers that expose endpoints for:
- On-demand embedding generation (``/embed``)
- Batch jobs (``/batch`` and job status)
- Model discovery (``/models``)
"""
