"""Model serving service package.

Layout:
- ``api``: REST endpoints for prediction and metadata.
- ``runtime``: model manager and service-scoped helpers.

Import convenience:
- from app.runtime.model_manager import ModelManager
"""
